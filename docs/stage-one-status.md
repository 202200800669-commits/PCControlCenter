# 第一阶段进度

## 已实现

仅迁移托盘与刷新间隔的配置导入、核心模型、适配器注册、身份精确匹配、通用监控、ThinkBook 只读接口、华硕与机械革命品牌发现、字段白名单诊断、模拟测试和 GitHub 构建工作流。图标和版式未制作，design 目录留作 Figma 交接。

## 本机验证

2026-09-15，Windows 普通权限：正确识别 LENOVO / 21R0 / ThinkBook 16p G6 IAX / R2CN57WW。内存占用、电量和亮度读取成功；风扇 WMI 访问被系统拒绝，返回专用错误码，未申请提权。新架构的风扇读取尚未在提权代理中验证。

旧版控制程序保留原位置与功能，此次没有替换运行文件或修改硬件设置。本地迁移基线记录了原型源码和既有测试哈希。

## 下一步

1. 搭建受限硬件代理和 IPC 验证，解决必要读取的权限。
2. 按顺序迁移风扇与性能模式写入并做真实回归。
3. 接入 Figma 设计和正式应用的配置存储。
4. 升级 LTS、确定许可证并完成公开测试包审核，再发布 GitHub。

当前完成第一阶段的可运行底座，第一阶段整体尚未完成，不能将此版本称为可替代旧控制台的新版。

## 构建与测试

Release 构建零警告、零错误。34 项模拟断言通过；本机打包后的 CLI 完成只读检测。测试包依赖 .NET 9 Runtime，尚未作为公开发行包。
