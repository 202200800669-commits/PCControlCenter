# 安全反馈

当前提供本地检测及参考 ThinkBook 的持续手动风扇控制。桌面程序保持普通权限；用户显式授权后，一个与客户端进程绑定的 UAC 代理服务于本次运行。代理核验命名管道两端进程、设备型号与 BIOS，仅接受固定类型的命令，不接受脚本、地址或 IOCTL 输入。关闭客户端连接会结束代理；退出后授权不跨运行保留。

持续风扇控制独占硬件会话，转速限定为已验证参考接口的 1500–5500 RPM。客户端持续发送带请求 ID 的目标与心跳，代理限制每次消息的等待时间，工作进程另设 10 秒心跳租约。断开、非法消息、停止、租约到期都会进入恢复路径。恢复会尝试向三个自动控制通道发送指令，并回读覆盖开关；这不等于全面证明固件自动策略恢复。

尚无独立安全审计或发布签名；强制结束整个进程树、固件失去响应或系统崩溃时不能保证恢复。关闭窗口会先停止手动控制，再按托盘偏好隐藏或退出；最小化和普通页面切换保留控制。

请不要在公开 Issue 附个人日志、凭据或可危害设备的详细利用步骤。涉及提权或硬件写入的问题，先通过仓库可用的私密安全报告渠道联系维护者；如未显示私密报告入口，仅公开描述影响范围，不附敏感细节。
